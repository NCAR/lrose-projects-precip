#! /bin/csh

cd $PROJ_DIR/export/scripts

running "RsyncSpol2UW.py --subDir rhi"
if ($status == 1) then
  RsyncSpol2UW.py --subDir rhi --realtime |& \
    LogFilter -d $ERRORS_LOG_DIR -p RsyncSpol2UW -i rhi &
endif

