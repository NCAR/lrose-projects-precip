#! /bin/csh

cd $PROJ_DIR/calibration/params

running "SunCal -params SunCal.spoldrx"
if ($status == 1) then
  SunCal -params SunCal.spoldrx |& \
    LogFilter -d $ERRORS_LOG_DIR -p SunCal -i spoldrx &
endif

