#!/bin/csh

# get sounding radar data

#cd $PROJ_DIR/ingest/scripts

setenv DATA_DIR '/home/snowband/brodzik/precip'
#running "GetCwbData.py3"

./GetCwbData.py3 \
    --verbose \
    --platform SOUNDING \
    --localDirBase $DATA_DIR/raw/sounding \
    --fileSuffix txt \
    --realtime \
    ##--lookbackSecs 3600 \
    --lookbackSecs 21600 \
    --realtime \
    --sleepSecs 60 
