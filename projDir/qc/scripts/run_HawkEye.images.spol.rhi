#! /bin/csh

# Get the file path from the command line

#set file_path = $1

# Ensure virtual X server is running and set display accordingly. 

start_Xvfb
set display_num = 1000
setenv DISPLAY :${display_num}.0

cd /home/rsfdata/git/lrose-projects-precip/projDir/qc/params
HawkEye -params HawkEye.images.spol_rhi -v

